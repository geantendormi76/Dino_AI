# requirements.in (V5 - 最终专业版)

# -----------------------------------------------------------------
# 核心AI与GPU支持 (!!! 确保 cu124 与你的CUDA版本匹配 !!!)
# -----------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.0
torchvision==0.20.0
torchaudio==2.5.0
onnxruntime-gpu  # ONNX GPU推理

# -----------------------------------------------------------------
# 核心机器学习与数据处理库
# -----------------------------------------------------------------
d3rlpy           # 离线强化学习库
gymnasium
filterpy         # 卡尔曼滤波器
numpy
onnx
scikit-learn
scipy

# -----------------------------------------------------------------
# 目标检测与图像处理
# -----------------------------------------------------------------
opencv-python
Pillow
ultralytics

# -----------------------------------------------------------------
# 工具与辅助库
# -----------------------------------------------------------------
mss              # 屏幕截图
pynput           # 键盘监听
tqdm             # 进度条
tensorboard      # 保留它，用于运行 tensorboard 命令
tensorboardX     #  d3rlpy 直接需要的依赖