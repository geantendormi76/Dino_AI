# dinoC 环境 (数据采集) 的最终、完整依赖蓝图
# 语法：--extra-index-url 必须在文件顶部单独声明

# 1. PyTorch 官方源 (用于安装CUDA版本的torch)
--index-url https://download.pytorch.org/whl/cu118

# 2. PaddlePaddle 官方源 (用于安装paddleocr)
--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu118

# 3. PyTorch 核心 (分类器所需)
#    安装时会自动使用上面的 --index-url
torch
torchvision

# 4. PaddlePaddle 核心 (OCR所需)
#    安装时会自动使用上面的 --extra-index-url
paddlepaddle-gpu==3.0.0
paddleocr==3.0.0

# 5. ONNX Runtime 核心 (YOLO检测器所需)
onnxruntime-gpu

# 6. YOLOv8 核心
ultralytics

# 7. 屏幕交互与工具库 (脚本直接依赖)
mss
pynput
opencv-python
numpy
tqdm